
CREATE TABLE people(
	id serial PRIMARY KEY,
	name varchar,
	age int,
	sex varchar,
	breed varchar
);

CREATE TABLE sheep(
	id serial PRIMARY KEY,
	name varchar,
	age int,
	sex varchar,
	breed varchar
);

CREATE TABLE pigs(
	id serial PRIMARY KEY,
	name varchar,
	age int,
	sex varchar,
	breed varchar
);

CREATE TABLE cows(
	id serial PRIMARY KEY,
	name varchar,
	age int,
	sex varchar,
	breed varchar
);